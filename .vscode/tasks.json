{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run",
      "type": "shell",
      "command": "pytest test -n auto --capture=no",
      "group": "test",
      "options": { "cwd": "${workspaceFolder}" }
    }
  ]
}
